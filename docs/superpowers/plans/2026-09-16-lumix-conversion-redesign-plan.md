# Lumix Studio conversion redesign implementation plan

1. Establish a clean baseline by running the existing structural checks and recording the current page/component behavior.
2. Add reusable homepage conversion sections and copy: clarified hero actions, trust strip, service-first ordering, project proof, five-step process, revised plans, concise founder trust, and final CTA.
3. Reuse the existing three concept demos and assets; keep explicit “Concepto Lumix” disclosure and avoid client claims.
4. Update shared plan content on the homepage and `/planes/` so both surfaces use the same decision headlines, truthful inclusions, and contextual contact URLs.
5. Extend the existing CSS system for the new hierarchy, improving mobile type sizes, touch targets, focus states, alignment, and reduced-motion behavior without adding dependencies.
6. Make the smallest JavaScript changes needed for project selection, menu behavior, motion-safe reveals, and contact prefilling.
7. Preserve all existing routes, metadata, schema, Netlify functions, sitemap entries, responsive images, contact behavior, and WhatsApp safety attributes.
8. Run syntax and structural checks, then test desktop, tablet, and mobile layouts and conversion paths in a local browser.
9. Package the reviewed site as a user-facing output. Do not deploy or publish.
